# Batch Manager

## Project structure

batch-manager/
├── package.json
├── server.js
└── public/
    └── index.html

## Run locally

npm install
npm start

## Render Web Service

Build Command:
npm install

Start Command:
npm start

Environment:
Node

No database is used yet. Current student/batch data is stored in the browser's localStorage.
